---
apiVersion: v1
kind: Service
metadata:
  name: hercules-http2mqtt
  labels:
    app: hercules-http2mqtt
    role: master
    tier: backend
spec:
  type: NodePort
  ports:
  - protocol: TCP
    port: {{ .http2mqttHttpPort }}
  selector:
    app: hercules-http2mqtt
    role: master
    tier: backend
  externalIPs:
    - {{ .external_ip }}
